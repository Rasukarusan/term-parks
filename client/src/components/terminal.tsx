import { makeStyles, Theme } from '@material-ui/core/styles'

const useStyles = makeStyles({
  container: {
    minHeight: '100vh',
  },
})

export const Terminal = () => {
  return <div>hoge</div>
}
